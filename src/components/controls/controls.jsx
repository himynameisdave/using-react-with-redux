import React, { Fragment } from 'react';
import './controls.css';

const Controls = () => (
    <Fragment>
        <button>
            Add Note
        </button>
        <button>
            Delete Note
        </button>
    </Fragment>
);

export default Controls;
